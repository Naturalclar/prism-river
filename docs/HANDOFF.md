# HANDOFF: Prism River を育てる

このリポジトリで作業するエージェントセッションへの引き継ぎ。

**v0 は Vite + TypeScript + React + oxlint + pnpm に移行済み**（2026-08-26）。下の「出発点」と「v0 で動いていること」は、リグレッションの基準として読む経緯の記録。

---

## 依頼

**Web 上の DAW（Logic Pro / BandLab のようなもの）がどこまで実用に足りるかを検証するための台を作る。** 音声ファイルをドラッグ＆ドロップで読み込み、マルチトラックで並べて再生・ミックスできるサイト。

**作業は全部ブラウザのローカルセッションで完結させること。** サーバーへのアップロードもアカウントも作らない。読み込んだ音声が端末から出ないことが前提で、これはプライバシー上の要件であると同時に「サーバー無しでどこまでいけるか」という検証テーマそのもの。

動く v0 は既にある。**ゼロから書き直すのではなく、それを起点にする。**

## 出発点

v0 は単一ファイル・依存ゼロ・ビルド不要の `index.html` だった（root commit `2ba3386`）。元は Artifact として公開したもの（https://claude.ai/code/artifact/ff441472-c1b9-427b-955b-813107502e48 ）で、このリポジトリに入れる際に名前と配色だけ変えてある。

**現在の正本は `src/` 以下。** v0 の挙動・見た目・計測値はそのまま移してあり、CSS は丸ごと `src/styles.css` に移動しただけ。v0 のコードを読みたいときは `git show 2ba3386:index.html`。

## v0 で動いていること（実機確認済み・2026-08-26）

Playwright で実際に音声を読ませて確認した範囲。**憶測ではなく計測値**なので、リグレッションの基準に使える。

- ドラッグ＆ドロップ／ファイル選択で**複数ファイル同時読み込み**、`decodeAudioData` でデコード
- Canvas でピーク波形描画（min/max を列ごとに畳む方式）。減衰4発の音源で振幅変化が出ることを確認済み
- 再生・停止・ループ、Space キー、ルーラークリックでシーク、`requestAnimationFrame` でプレイヘッド追従
- トラックごとに音量（GainNode）・パン（StereoPannerNode）・ソロ・ミュート・削除
- **クリップの横ドラッグで開始位置をずらせる**。再生中にずらすと即座にソースを組み直す
- マスター音量、AnalyserNode による L/R レベルメーター、ズーム（px/秒）
- `OfflineAudioContext` でミックスを一括レンダー → WAV エンコード

**計測結果（テスト音源、44.1kHz ステレオ）:**

| 項目 | 値 |
| --- | --- |
| sampleRate | 44,100 Hz |
| 出力レイテンシ | 32.0 ms |
| デコード | 2ファイルで 18 ms |
| PCM メモリ | 2トラック 4秒で 1.3 MB |
| オフラインレンダー | 4秒のミックスを **12 ms＝実時間の約323倍速** |

最後の行がこの検証の一番の収穫。**ミックスダウンは実時間の数百倍で回る**ので、書き出しの速度はブラウザだからという理由でネックにはならない。

## 既知の制約（v0 で判明済み。作り直しても消えない）

1. **`.wav` は claude.ai の Artifact ビューア上では保存できない。** ビューアの保存許可拡張子が `png jpg webp mp4 webm txt json md`（拡張セットでも `docx pptx csv svg pdf` 等）で、音声拡張子が一つも無い。**ブラウザの制限ではなくビューア側の制限**なので、**自分のドメインに置けば普通に保存できる**。v0 は `window.claude` の有無を見てビューア内なら `downloads.save()`、素で開かれたら通常のダウンロードに落とす作りになっている。リポジトリ化して自前ホストにすれば、この分岐の後半だけが効く。
2. PCM はデコード後 **Float32 で持つ**ので、`長さ(秒) × 44100 × ch × 4 bytes`。**10分ステレオ1本で約 202MB**（この行は当初 100MB と書かれていたが #21 の実測で誤りと判明。100MB は5分の値）。トラックが増えるとメモリが線形に増える——ただし #21 の実測では 11.3GB まで破綻せず、先に効くのはデコード時間だった。

## 済み

1. ~~**ビルド構成を決めて移す。**~~ → Vite + TS + React + oxlint + pnpm。「決めたこと」参照。
2. ~~**テストを置く。**~~ → vitest（`src/lib/` の純粋関数3つ）と Playwright（読み込み／再生／停止／書き出し／削除）。`.github/workflows/ci.yml` で lint → typecheck → unit → E2E を回す。E2E のテスト音源は `src/lib/wav.ts` でその場生成しているので、権利のあるファイルはリポジトリに入らない。
3. ~~**GitHub Pages へのデプロイ。**~~ → #5。main への push で公開される。`vite.config.ts` の `base: "./"` のままなので、後から Cloudflare Worker 経由で smashcat.dev 配下に載せ替えることもできる。
4. ~~**v0 由来のバグ4件。**~~ → #1（長尺・高ズームで canvas が一辺上限 65535px を超えて白紙になる）、#2（`formatTime` が分境界で `00:60.00` を返す）、#3（テーマ切り替えでルーラーが再描画されない）、#4（`bounce()` が失敗すると書き出しボタンが永久に無効化される）。**どれも「例外が出ないまま壊れる」型**だったので、同種を見つけたら同じ粒度で Issue にすること。

## 進め方 — まず Issue 化してから着手する

**下のバックログをいきなり実装しないこと。** 順番はこう:

1. **バックログを GitHub Issue にする。1項目 = 1 Issue。** まとめて一気に立てて構わない。本文は「**何が起きるか / なぜ問題か / 提案 / 確認方法**」の4節で書く——曖昧な要望ではなく、読んだ人がそのまま着手できる粒度にする。**既に Issue がある項目は下に番号を書いてあるので、重複を立てないこと。** 立てる前に `list_issues` で現況を確認する。
2. **着手は Issue 単位。** 1 Issue = 1 ブランチ = 1 PR。PR 本文に `Closes #N` を書く。
3. **`decision-needed` が付いた Issue は実装を始める前に本人の判断を仰ぐ。** Issue にコメントで選択肢と推奨を書いて止まる。勝手に決めて進めない。
4. **測ったことは README のテレメトリ表に足す。** この台の存在理由が数字を出すことなので、機能を足したら前後の数字を並べる。

ラベルの目安: `infra` / `feature` / `wasm` / `measurement` / `decision-needed`

Issue を立てる前に **`## 制約` と `## 決めたこと` を必ず読む。** そこに書いてある前提を崩す提案は、Issue の時点で弾く。

## バックログ（この順で Issue にする）

### 0. 先に決めるもの（他が待っている）【済み】

- ~~**LICENSE を置く**~~ → **#19**。**GPL-3.0 に決定**（本人判断・2026-08-26）。「決めたこと」参照。

### 1. DAW として成立させる（WASM は要らない）【Issue 済み】

Web Audio の標準ノードで足りる範囲。ここを先に潰す。**この段は全部 Issue になっているので、新しく立てずに着手する。**

- **クリップのトリム** → **#10**。左右端をドラッグして頭尻を詰める。DAW として最低限これが要る。この段の筆頭
- **フェードイン / アウト** → **#11**。`GainNode` の `linearRampToValueAtTime`。トリムとグリップ位置が競合するので #10 の後
- **エフェクト** → **#12**。`BiquadFilterNode`（EQ）と `DynamicsCompressorNode`。ここが「Logic の代わりになるか」の分水嶺
- **グループバス** → **#13**。弦 / 管 / 鍵盤 の3系統（「命名と構造」参照）。#12 のエフェクトを挿す先
- **録音入力** → **#14**。`getUserMedia` + `MediaRecorder`
- **トラックの選択と Delete キー削除** → **#9**。上記に含まれていなかったが、操作の当たり判定が #10 と競合するので順番を合わせること

### 2. WASM を1つだけ入れて測る【済み】

- ~~**MP3 / Opus 書き出し**~~ → **#20**。**MP3（LAME の WASM ビルド・`wasm-media-encoders`）で実装済み。** Issue 起票時は libopus 先行の想定だったが、着手時点で (a) Opus は #39 の webm 書き出しで出せるようになっていた、(b) LICENSE が GPL-3.0 に決まり LAME（LGPL）の縛りが消えた、ので MP3 を選んだ。計測結果（3秒: レンダー 10ms + エンコード 81ms＝約37倍速。**数百倍速は維持できず数十倍速に落ちるが実用上は問題ない**）は README の計測節に記載。エンコードは Worker + WASM（シングルスレッド）で、メインバンドルには乗らない

### 3. 検証して README に答えを書く【Issue 済み】

計測が成果物。分かったことを README に残すまでを1件とする。

- ~~**長尺の実測**~~ → **#21**【実測済み】 — README の計測節に結果を置いた。**この環境（15GB）では破綻しなかった**: 10分×56本（音声9時間20分・PCM 11.3GB）まで完走し、破綻点はアプリ側ではなく端末の物理メモリで決まる。先に効くのはメモリよりデコード時間（10分×40本で84秒）。`pnpm measure:long` で再現できる
- **コーデックの実対応** → **#22**【Chromium 分は実測済み】 — README に対応表を置いた。Chromium 列（wav / mp3 / ogg-Vorbis / webm-Opus ✔）は E2E で毎 CI 検証される。対応外ファイルを黙って落とす問題も修正済み（名前を挙げて伝える）。**残り: Safari / Firefox / m4a / flac を実機と手持ち音源で埋める**（#23 のモバイル実機検証と合わせてやるのが早い）
- **モバイル Safari** → **#23** — iOS は AudioContext の起動にユーザー操作が要る、バックグラウンドで止まる等の制約が強い。実機でどこまで動くか
- **レイテンシの実用性** → **#24** — 録音入力（#14）を足したとき、モニタリングの遅延が演奏に耐えるか。出力レイテンシ 10〜32ms がこの判断の入口

### 3.5 MIDI（#46・実装済み）

- ~~**MIDI ファイルの取り込み**~~ → **#46**【段1 実装済み】 — SMF を自前解析（`src/lib/midi.ts`・依存ゼロ・単体テスト13件）し、内蔵シンセ（`src/audio/midi.ts`）でオフラインレンダーしてチャンネルごとに1本のオーディオトラックにする。AudioBuffer にした時点で下流（トリム / FX / バス / 書き出し / 保存）は無改造で効く。保存・復元も MIDI 経路を通すよう直してある（`importProject`）。計測は README（1,000音・4ch で約7倍速）。**段2（SoundFont / FluidSynth でまともな音色、GM→バス自動割り当て、Web MIDI 入力、ピアノロール）は未着手で、必要になったら別 Issue。**

### 4. あとで（Issue は立ててよいが着手は後）

- **タイムストレッチ / ピッチシフト** → **#25** 【`wasm`】 — `playbackRate` は**テンポとピッチが連動する**ので素の API では不可能。`SoundTouch`（LGPL）で解決する。用途がはっきりしている: 練習用に曲だけ遅くする
- **BPM / キーの自動検出** → **#26** 【`wasm`】【`decision-needed`】 — `Essentia.js` が該当するが **AGPL**。LICENSE の決定次第で可否が変わる
- **音源分離（ボーカル / 伴奏）** → **#27** 【`wasm`】 — ONNX Runtime Web でモデルを回す。一番重いが、**「サーバー無しでどこまでいけるか」の限界を測る題材としては最適**。うまくいかなくても「ここが限界だった」が成果になる
- ~~**実時間 webm 書き出し**~~ → **#15**（実装済み・PR #39）。`MediaStreamAudioDestinationNode` + `MediaRecorder`。段2 の MP3（WASM・オフライン）と役割分担が付いた: webm/Opus は素の API で実時間、MP3 は WASM で数十倍速

## WASM を入れるときの前提

**「遅いから WASM」ではない。** v0 の実測でオフラインレンダーは実時間の約309倍速、デコードは2ファイル9ms、波形のピーク計算も引っかかっていない。**速度目的で入れても速くならず、バイナリだけ増える。**

WASM の領分は **Web Audio API に機能として無いもの**:

| Web Audio にある（WASM 不要） | 無い（WASM の出番） |
| --- | --- |
| ゲイン、パン、EQ、コンプ、リバーブ、解析、オフラインレンダー | タイムストレッチ、ピッチシフト、任意コーデックのエンコード、BPM / キー検出、音源分離、任意の DSP |

**やらない方がいいもの**（提案されても Issue の時点で弾く）:

- **波形ピーク計算の WASM 化** — 今の実装で引っかかっていない。min/max の畳み込みは JIT がよく効く形
- **ミックスダウンの WASM 化** — 309倍速で回っている。速くしても体感は変わらない
- **`ffmpeg.wasm` を「とりあえず」入れる** — 何でもできる代わりに数十MB。やりたいことが1つなら、そのライブラリを単体で持ってくる方が軽い

**実装前に確認が要る点:**

- **SharedArrayBuffer（＝WASM のスレッド）にはクロスオリジン分離が要る。** COOP `same-origin` と COEP `require-corp` のヘッダが必要で、**GitHub Pages は独自ヘッダを付けられない**。medley-generator と同じ Pages + Cloudflare Worker 構成なら Worker 側で足すことになる。**まずシングルスレッド + WASM SIMD で測り、足りなければスレッドを検討する順番。**
- **メモリの二重持ち。** PCM は Float32 で保持していて 10分ステレオ1本で約202MB（#21 実測）。WASM 側にコピーを作るとさらに倍になるので、同じ `ArrayBuffer` を見せるゼロコピーの形を最初から考える
- **ライセンス。** BSD 系（`libopus` / `libFLAC`）が一番安全。LGPL（`LAME` / `SoundTouch`）は wasm が実質静的リンクなので再リンク可能性の要件をどう満たすか調べる必要がある。GPL / AGPL（`aubio` / `Essentia` / `Rubber Band`）はリポジトリ全体を縛る。**各ライブラリの条件は自分で一次情報を確認すること**——ここの記述は出発点であって根拠ではない

## 決めたこと（2026-08-26・決着済み）

**Vite + TypeScript + React + oxlint + pnpm。** 本人の判断。

引き継ぎ時点の推奨は「React 無し」だった——プレイヘッドが毎フレーム動く UI に仮想 DOM を挟む利点が薄い、というのが理由。**採らなかった。** medley-generator と同じ構成に揃える方が、道具立てを一つ覚えれば両方触れるという利点が上回る。個人プロジェクトを2つ別々の作りで持つコストの方が、この規模では高い。

推奨側の懸念（再描画コストが計測に乗る）は、**構造で外してある**:

- `src/audio/engine.ts` の `Engine` が AudioContext・トラック・トランスポートを全部持つ。React には依存しない（#40 で肥大化を分割: グラフ構築は `audio/graph.ts`、書き出しは `audio/bounce.ts`、録音は `audio/recorder.ts`、保存メタは `audio/project.ts`、型と定数は `audio/types.ts`。`Engine` は公開 API を保つファサードで、コンポーネントの import 元は従来どおり engine.ts）
- React は `useSyncExternalStore` で**スナップショット**（AudioNode を含まない素のデータ）を読むだけ
- プレイヘッド・時計・レベルメーターは `engine.onFrame()` を購読して **DOM を直に書き換える**。毎フレームの再レンダーは発生しない
- クリップの横ドラッグ中も `style.left` を直書きし、指を離したときだけ `commitOffset()` で React と再生グラフを組み直す

移行前後でオフラインレンダーの倍率が変わっていないこと（約323倍速 → 約309倍速、環境差の範囲）を実測で確認済み。**この構造を崩すと計測値が信用できなくなる**ので、オーディオのタイミングに関わるものを `useState` に持たせないこと。

リンタは medley-generator に合わせて **oxlint**（ESLint ではない）。**oxlint はデフォルトで警告時も exit 0 になる**ので、`lint` スクリプトには `--deny-warnings` を付けてある（medley-generator 側で実際に踏んだ罠）。

**ライセンスは GPL-3.0**（#19・本人判断・2026-08-26）。`aubio` / `Rubber Band` など GPL の音楽資産を WASM で持ち込む可能性を残す選択。含意:

- BSD 系（`libopus` / `libFLAC`）と LGPL（`LAME` / `SoundTouch`）はそのまま使える。GPL（`aubio` / `Rubber Band`）も使える
- **AGPL（`Essentia.js`）だけは別扱いのまま**。GPLv3 と AGPLv3 は組み合わせ可能だが条件が複雑になるので、#26 で使うなら改めて確認する
- このリポジトリのコードを流用する側には GPL が伝播する。permissive に戻す判断をするなら、外部コントリビューションを受け入れる前に

## 制約

- **音声を端末から出さない。** アップロード先を作らない。解析のための外部 API 呼び出しもしない
- **依存を安易に増やさない。** 波形描画も WAV エンコードも Web Audio と Canvas だけで足りている。ライブラリを入れるなら「自前だと何が足りなかったか」を先に書くこと
- **v0 の計測値を消さない。** 下段のテレメトリ（sampleRate / レイテンシ / デコード時間 / PCM メモリ / レンダー時間）はこの台の存在理由なので、UI を作り直す場合も同等の情報を残す
- **オーディオの状態を React に持たせない。** 上の「決めたこと」の通り、`Engine` が正本でコンポーネントは表示だけ
- 公開リポジトリなので、テスト用音源に**権利のあるファイルをコミットしない**（`.gitignore` で音声拡張子を弾いてある。`git add -f` で押し込まない）
- **依存はライセンス互換を確認してから足す。** 本体は GPL-3.0（#19）。BSD / LGPL / GPL は互換、AGPL（`Essentia.js`）は要個別確認

## 完了条件（v1）

- **LICENSE が置かれている**
- 音声を読み込み → 並べ替え → トリム → ミックス書き出し、が一気通貫で動く
- 書き出しが WAV 以外にも出せる
- 単体テスト（ピーク計算・WAV エンコード・時間変換）と E2E（読み込み〜レンダー）が CI で回る
- README にバックログ3の検証の答えが埋まっている
- **バックログの各項目が Issue として存在し、閉じたものは PR に紐づいている**

## 命名と構造

Prism River は騒霊三姉妹から。**ルナサ＝弦 / メルラン＝管 / リリカ＝鍵盤**の編成をそのまま構造に借りている。

- トラックの先頭3色がこの3人に対応する（`src/audio/engine.ts` の `HUE`）
- 「これから」4番目の**グループバスもこの3系統で切る**予定。トラックを弦/管/鍵盤のサブミックスにまとめ、そこにエフェクトを挿す形

装飾ではなく編成の呼び名として使っているので、バスを実装するときはこの対応を崩さないこと。

## 参考

- `Naturalclar/medley-generator` — 同じ人の既存 Web アプリ。技術構成（Vite + React + TS + oxlint + vitest + Playwright）とデプロイ（GitHub Pages を Cloudflare Worker 経由で smashcat.dev に載せる）の前例
- kanban リポジトリの `misc/dtm-plugins.md` — 本人が普段使っている DTM 環境。何が「代わりになる」かの基準
- kanban リポジトリの `brainstorm/dev/wasm-audio-web.md` — 上の「WASM を入れるときの前提」の元になった検討。候補ライブラリの一覧と、なぜ MP3/Opus 書き出しを最初に置くかの理由はこちらに詳しい
